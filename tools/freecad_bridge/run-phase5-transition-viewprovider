#!/usr/bin/env bash
set -euo pipefail

bridge_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
project_root="$(cd "${bridge_dir}/../.." && pwd)"

exec "${bridge_dir}/run-isolated" \
    /usr/bin/env -i LANG=C.UTF-8 LC_ALL=C.UTF-8 \
    /usr/bin/python3 -I -S -B \
    "${bridge_dir}/run_phase5_transition_viewprovider.py"
